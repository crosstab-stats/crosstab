{
  "name": "crosstab",
  "version": "0.0.0",
  "description": "Statistics for everyone, every device, everywhere — a browser-based stats tool (WebR + DuckDB-WASM).",
  "license": "Unlicense",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test"
  }
}
